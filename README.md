# Craft Demo Model
Jupyter notebooks that demonstrate how the raw data is cleaned, pre-processed and splitted for training 
then selecting process of models and hyperparameters and finally storing the trained the model. 

## Getting Started

```bash
uv sync
uv run jupyter lab  # or uv run jupyter notebook
```